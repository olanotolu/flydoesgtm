# Server-only Clay Public API configuration.
# Never put a real key in browser code or commit a populated .env file.
CLAY_PUBLIC_API_KEY=
CLAY_WEBHOOK_SIGNING_SECRET=
# Optional overrides for testing against a compatible endpoint.
# CLAY_API_BASE_URL=https://api.clay.com/public/v0
